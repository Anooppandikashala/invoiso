version: 1

AppDir:
  path: ./AppDir
  app_info:
    id: com.invoiso
    name: Invoiso
    icon: logo
    version: 1.0.0
    exec: invoiso
  runtime:
    env:
      PATH: $APPDIR/usr/bin:$PATH

AppImage:
  arch: x86_64
  update-information: None
  sign-key: None
